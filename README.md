# 🌳 TreeMenu — Django App для древовидных меню

**TreeMenu** — это Django-приложение, реализующее древовидное меню с неограниченной вложенностью, управляемое через Django Admin.  
Меню можно вставлять в любое место шаблона с помощью тега:

```html
{% load tree_menu_tags %}
{% draw_menu 'main_menu' %}
```

# 📍 Поддерживает:

- хранение меню в базе данных
- редактирование через админку
- определение активного пункта по текущему URL
- разворачивание веток до активного элемента
- поддержку нескольких меню по названиям
- указание URL как прямым путем (/catalog/), так и через named url
- ровно 1 запрос к базе данных при рендере меню
- автоматическую загрузку фикстур при запуске Docker

# 📂 Структура проекта

```textplain
core/
  __init__.py
  asgi.py
  settings.py
  urls.py
  wsgi.py
docker-compose.yml
Dockerfile
entrypoint.sh
manage.py
requirements.txt
templates/
  tree_menu/
    index.html
    menu_item.html
    menu.html
tree_menu/
  __init__.py
  admin.py
  apps.py
  fixtures/
    initial_menu.json
  migrations/
    __init__.py
    0001_initial.py
  models.py
  templatetags/
  tests.py
  urls.py
  views.py
```


# 🐳 Запуск через Docker
1️⃣ Собрать контейнер 


``` bash
docker-compose build
```

2️⃣ Запустить
```bash
docker-compose up
```

При запуске Docker автоматически выполнит:

- миграции

- загрузку фикстур initial_menu.json

- запуск runserver
